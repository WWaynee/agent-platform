# 需求单 0004：前端网页（纯 HTML + 原生 JS）+ 登录体验改造

- 状态：✅ **已完成并验证**（2026-08-19 实现 + 后端单测/全量回归 + 独立/安全审查通过；已提交推送 `origin/main`）
- 优先级：🔴 高（项目目前只有后端 API，没有可用的前端界面；这是把系统变成"可演示产品"的关键一步）
- 模块：`web/`（新增前端静态资源）、`api/handler/user.go` + `api/service/user.go` + `storage/user.go`（登录体验改造）、`storage/model/models.go`（user 脱敏）、`api/handler/tenant.go`（租户详情归属校验补强）、`deploy/`（Nginx 参考配置）
- 创建日期：2026-08-19
- 完成日期：2026-08-19

---

## 一、功能现状

当前系统**只有后端 API**（Gin 服务，`cmd/api/main.go`），没有任何前端页面。所有能力（登录 / 会话 / 对话 / 文档 / 工具配置 / 用量）都只能靠 curl 或 Postman 调用，无法作为 SaaS 产品演示。

## 二、前端方案（已与需求方确认）

### 2.1 技术选型（极简方案，零构建）

| 技术 | 选型 | 理由 |
|------|------|------|
| 框架 | 纯 HTML + 原生 JS | 不引入 React/Vue，部署简单、好调试 |
| 样式 | Tailwind CSS CDN | 不用本地构建，直接 CDN 引入 |
| 图标 | Font Awesome CDN | 图标够用 |
| 打包 | 无构建 | 静态文件直接扔 Nginx |
| 交互 | fetch API + 原生 DOM | 不用 axios |

### 2.2 页面结构（3 个页面）

1. **login.html**：登录（用户名+密码）+ 租户注册（创建租户+管理员），居中卡片、tab 切换。
2. **index.html**：三栏主工作台
   - 顶栏：租户名 / 用户名 / 角色 / 管理入口（仅 admin 可见）/ 退出
   - 左栏：会话列表（新建 / 点击切换 / 删除）
   - 中栏：对话消息流（用户右、AI 左）+ 输入框；展示工具调用过程（可选加分项）
   - 右栏：文档管理（拖拽/点击上传、文档列表、状态轮询、删除）
3. **admin.html**：管理后台（仅 admin 可见入口）
   - 工具开关配置（`GET/PUT /api/admin/tool-config*`）
   - 用量统计（当天 + 最近 N 天）

> **本轮不做用户管理**（需求方已确认）：后端无管理员创建用户接口，admin 页只做工具配置 + 用量统计。

### 2.3 鉴权与 API 封装

- 登录成功把 JWT 存 `localStorage`，每次请求带 `Authorization: Bearer <token>`。
- `api.js` 统一封装：`res.status === 401` → 跳登录页；**业务错误（HTTP 200 + `body.code !== 0`）也要判断**（本项目业务错误统一 HTTP 200 + code 区分，见「接口支撑对照」第 6 条）。
- 登录后缓存 `user`（username / tenant_id / role）与租户名到 localStorage，页面刷新免重复拉取。

## 三、登录体验改造（后端配合，需求方已确认）

需求方要求：**登录只输用户名 + 密码，不输 tenant_id 也不输租户名**。当前 `POST /api/user/login` 的 `tenant_id` 是 `binding:"required"`，必须调整。

### 3.1 后端改动

**1. `LoginReq.TenantID` 改为可选（`api/handler/user.go`）**

```go
type LoginReq struct {
	TenantID uint64 `json:"tenant_id" binding:"omitempty"`            // 可选：传了则按租户+用户名查；不传按用户名全局登录
	Username string `json:"username" binding:"required,min=2,max=64"`
	Password string `json:"password" binding:"required,min=6,max=128"`
}
```

**2. `service.Login` 支持"用户名全局登录"（`api/service/user.go`）**

- `tenantID > 0`：保持现状（按租户 + 用户名查，兼容老调用方）。
- `tenantID == 0`：按用户名全库查询（新增 `storage.FindUsersByUsername`）：
  - **恰好 1 个用户** → 直接登录（签发 JWT 用该用户的 tenant_id）；
  - **多个用户** → 返回明确错误「该用户名在多个租户下存在，无法自动登录，请提供租户 ID」；
  - **0 个** → 统一「用户名或密码错误」（不区分用户不存在/密码错误，防探测，延续现状）。

> ⚠️ 边界说明（已与需求方确认方案）：`users.idx_tenant_user` 联合唯一索引决定**不同租户可共存同名用户**（README 实测"租户 A/B 可共存同名 admin"）。纯用户名登录遇到同名冲突无法消除歧义，只能返回错误引导——这是该方案的固有边界，需求方接受。

**3. 登录 / 注册响应脱敏（`storage/model/models.go`）**

- `User.PasswordHash` 加 `json:"-"`：登录、注册、注册租户等接口返回 `user` 对象时不再泄露密码哈希（README 红线：敏感信息不外泄）。
- 其余字段（ID/TenantID/Username/Role/Status/时间）不受影响。

**4. 新增 `storage.FindUsersByUsername`（`storage/user.go`）**

```go
// FindUsersByUsername 按用户名全库查询（可能命中多个租户的同名用户）
func FindUsersByUsername(ctx context.Context, username string) ([]model.User, error)
```

### 3.2 前端适配

- login.html 登录表单只有「用户名 + 密码」两个输入框；提交 `POST /api/user/login` **不传 tenant_id**。
- 同名冲突时后端返回 400 明确错误，前端 toast 展示该提示（此时用户可改用租户注册页重新注册唯一用户名，或由管理员处理）。
- 注册页：`POST /api/tenant/register` 成功后**不返回 token**，前端自动用刚注册的 admin 账号调一次登录完成免手工输入。

## 四、前端页面设计

### 4.1 文件结构（`web/` 新增目录）

```text
web/
├── login.html       # 登录 / 租户注册
├── index.html       # 主工作台（三栏）
├── admin.html       # 管理后台
├── css/
│   └── style.css    # 少量自定义样式（滚动条 / 消息气泡 / 骨架屏等）
└── js/
    ├── api.js       # fetch 封装：带 token、401 跳登录、code!=0 抛错
    ├── auth.js      # token 存取 / 登录态校验 / 用户信息缓存
    ├── chat.js      # 会话列表 + 对话逻辑
    ├── document.js  # 文档上传 / 列表 / 删除 / 任务轮询
    └── admin.js     # 工具开关 + 用量统计
```

### 4.2 index.html 交互细节

| 区域 | 交互 | 后端接口 |
|------|------|---------|
| 顶栏 | 登录后调 `GET /api/tenant/:id` 拿租户名（登录响应 user 只有 tenant_id）；admin 显示「管理后台」入口 | `GET /api/tenant/:id` |
| 左栏 | 新建会话 `POST /api/session`；列表 `GET /api/session/list`；切换会话加载历史 `GET /api/session/:id/messages`；删除 `DELETE /api/session/:id` | 见左 |
| 中栏 | 发送 `POST /api/chat`（携带当前 session_id，空则后端自动建会话）；工具调用过程展示：chat 响应 `tool_calls` 只有工具名，发送后**再刷新一次历史接口**拿到 `kind=tool_call/tool_result` 的完整过程 | `POST /api/chat` + `GET /api/session/:id/messages` |
| 右栏 | 上传 `POST /api/document/upload`（FormData 字段 `file`）→ 拿 `task_id` 轮询 `GET /api/task/:id`（间隔 2s，防撞限流）→ 完成后刷新列表；列表 `GET /api/document/list`；删除 `DELETE /api/document/:id` | 见左 |

> ⚠️ 已知限制（实现时前端做提示，不属本轮后端改动）：
> - **删除文档仅上传者本人可删**（用户级隔离）：member 删他人文档返回 400「无权删除他人文档」，前端删除按钮对非本人文档置灰或失败 toast。
> - **无流式输出**：后端 chat 为普通 JSON 全量返回，前端等待完整回答后渲染。
> - **限流**：用户级 60 次/分、对话级 20 次/分；任务轮询间隔 ≥2s，避免高频轮询撞 429（429 时退避重试）。

### 4.3 admin.html 细节

| 区域 | 交互 | 后端接口 |
|------|------|---------|
| 工具配置 | 渲染开关列表（`tool_name / is_enable / description`），toggle 调 `PUT /api/admin/tool-config/:tool_name` | `GET /api/admin/tool-config`、`PUT ...` |
| 用量统计 | 当天 `{tokens, calls}` + 最近 7 天表格（`GET /api/admin/usage/history?days=7`），可用简单 CSS 柱状条 | `GET /api/admin/usage/today`、`GET /api/admin/usage/history` |

### 4.4 Nginx 参考配置（`deploy/nginx.conf`）

```nginx
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html/web;   # 前端静态文件（web/ 目录挂载位置）
    index login.html;

    # API 反向代理到后端 Gin（默认 8080）
    location ^~ /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /health {
        proxy_pass http://127.0.0.1:8080;
    }
}
```

## 五、接口支撑对照（逐项核对现有后端代码）

> 结论：**除登录的 tenant_id 必填项外，前端方案所需的全部接口均已存在**，无需新增业务接口。下表为逐项核对结果（依据 `api/router.go` 与各 handler 实码）。

| 前端功能 | 后端接口 | 核对结果 |
|---------|---------|---------|
| 租户注册（建租户+管理员） | `POST /api/tenant/register` `{name, admin_username, admin_password}` → `{tenant, admin}` | ✅ 已存在（需求单 0001） |
| 登录 | `POST /api/user/login` `{tenant_id, username, password}` | ⚠️ **tenant_id 必填需改造**（见第三节） |
| 新建会话 / 列表 / 删除 | `POST /api/session` / `GET /api/session/list` / `DELETE /api/session/:id` | ✅ 已存在 |
| 会话历史（含工具过程） | `GET /api/session/:id/messages` → `{messages:[{role,content,kind}]}` | ✅ 已存在（需求单 0002 冷轨） |
| 对话 | `POST /api/chat` `{session_id, query}` → `{answer, session_id, tool_calls}`（空 session_id 自动建会话） | ✅ 已存在 |
| 文档上传 / 列表 / 删除 | `POST /api/document/upload`（FormData `file`，返回 `task_id`）/ `GET /api/document/list` / `DELETE /api/document/:id` | ✅ 已存在 |
| 任务状态轮询 | `GET /api/task/:id`（pending/processing/success/failed + error_msg） | ✅ 已存在 |
| 工具开关 | `GET /api/admin/tool-config`、`PUT /api/admin/tool-config/:tool_name` | ✅ 已存在（admin 组） |
| 用量统计 | `GET /api/admin/usage/today`、`GET /api/admin/usage/history?days=N` | ✅ 已存在（admin 组） |
| 租户名展示 | `GET /api/tenant/:id`（私有，需登录） | ✅ 已存在（登录后调用） |
| 管理员创建用户 | — | ❌ **无此接口**，本轮不做用户管理（需求方已确认） |
| 流式输出（SSE） | — | ❌ 后端无 SSE，本轮不做流式（需求方已确认可接受） |

### 前端需特别注意的后端约定

1. **统一返回结构**：`{code, message, data}`；成功 `code=0`、HTTP 200。业务错误（400/403/500）**HTTP 200 + code 非 0**；仅鉴权失败（401）是真正 HTTP 401。`api.js` 必须同时判断 `res.status === 401` 与 `data.code !== 0`——401 时登录接口的服务器 message（如"多租户同名"）会透传给前端展示，业务页面的 401 才跳登录。
2. **403 语义**：`response.Forbidden` 走 HTTP 200 + code=403（如越权访问他人会话）。
3. **会话标题**：`POST /api/session` 可不传 title（默认「新会话」）；chat 接口空 `session_id` 自动建会话，标题取首问前 20 字。
4. **文档删除用户级隔离**：仅上传者可删。
5. **限流**：全局限流（租户 300/分、用户 60/分）+ 对话专属（20/分）+ 配额拦截；429 触发需退避。

## 六、涉及文件清单

| 文件 | 改动类型 |
|------|---------|
| `update/需求单-0004-前端网页与登录体验.md` | **新增**（本文档） |
| `api/handler/user.go` | `LoginReq.TenantID` 改 `omitempty` 可选 |
| `api/service/user.go` | `Login` 支持 tenantID==0 用户名全局登录（唯一即登录 / 同名报错） |
| `storage/user.go` | **新增** `FindUsersByUsername` |
| `storage/model/models.go` | `User.PasswordHash` 加 `json:"-"` 脱敏 |
| `api/handler/tenant.go` | **补强（安全审查修复）**：`GetTenantDetail` 增加租户归属校验——路径 id 必须等于 JWT tenant_id，否则返回"租户不存在"，堵住「任意登录用户可查任意租户详情」的既有 IDOR（登录态前端会常态调用它拉租户名） |
| `api/service/account_test.go` | **新增/更新**：纯用户名登录（唯一/同名冲突/不存在）用例 |
| `web/login.html` | **新增**：登录 + 租户注册 |
| `web/index.html` | **新增**：三栏主工作台 |
| `web/admin.html` | **新增**：管理后台 |
| `web/css/style.css` | **新增**：少量自定义样式 |
| `web/js/api.js` | **新增**：请求封装 |
| `web/js/auth.js` | **新增**：鉴权 / 登录态 |
| `web/js/chat.js` | **新增**：会话 + 对话 |
| `web/js/document.js` | **新增**：文档管理 |
| `web/js/admin.js` | **新增**：管理后台逻辑 |
| `deploy/nginx.conf` | **新增**：Nginx 反代参考配置 |
| `README.md` | 前端目录 / 启动方式 / 待补齐项更新 |

## 七、自测任务

### 7.1 后端自测（`go test ./...`）

- [x] 纯用户名 + 密码登录：用户名全库唯一 → 成功签发 JWT（token 含正确 tenant_id/user_id）。（单测 `TestLogin_ByUsername_Unique`）
- [x] 纯用户名登录遇**同名冲突**（两个租户各有 admin）→ 400「该用户名在多个租户下存在」。（单测 `TestLogin_ByUsername_Duplicate`）
- [x] 纯用户名登录**用户名不存在** → 401「用户名或密码错误」（不区分存在性，防探测）。（单测 `TestLogin_ByUsername_NotFound`）
- [x] 带 tenant_id 登录（老调用方式）→ 行为不变，回归通过。（既有 `Login` 用例 + `TestLogin_DisabledTenantRejected` 等保留，`go test ./...` 全绿）
- [x] 登录响应 `user` 对象**不再包含 PasswordHash 字段**（脱敏生效）。（单测 `TestUser_PasswordHashOmitted`）
- [x] `go vet ./...` / `go build ./...` 全绿。（实测 `go vet ./...` 全绿、`go build ./...` 编译通过）

### 7.2 前端自测（手工/浏览器）

> ⚠️ **如实标注：以下均为前端浏览器手工 e2e，尚未在此环境跑真实浏览器**。代码已通过 `node --check` 语法校验 + 独立 review 逐接口字段映射核对（`web/admin.html` 仅管理员可见、401 拦截等逻辑在代码中实现），但**需在本地起服务 + 浏览器实测后再打勾**。

- [ ] login：注册租户 → 自动登录进入工作台。
- [ ] login：用户名+密码登录成功；错误密码 401 toast；token 失效（手动改坏 localStorage）自动跳回登录页。
- [ ] index：新建会话 → 发送消息 → AI 回答渲染（用户右/AI 左）；切换会话历史正确；删除会话。
- [ ] index：工具调用过程展示（对话后历史含 tool_call/tool_result）。
- [ ] index：上传文档 → 轮询任务状态（pending→processing→success）→ 文档列表状态更新；删除文档（本人）。
- [ ] index：member 删除他人文档 → 失败提示（无权删除他人文档）。
- [ ] admin（仅 admin 角色可见入口）：工具开关 toggle 生效；用量当天/历史展示。
- [ ] 401 拦截：未登录访问 index.html → 跳转 login.html。

### 7.3 回归

- [x] 既有 e2e 脚本依赖登录接口的（`scripts/e2e_*.sh`）不受影响——登录新增"不传 tenant_id"分支，**老调用方式完全兼容**。（代码层确认：登录授权兼容路径保留，`tenantID>0` 走原逻辑；已核对全部 `scripts/e2e_*.sh` 登录调用均显式传 `tenant_id`，走兼容路径。⚠️ e2e 脚本本身未在此环境实跑）
- [x] 多租户隔离链路不回归（本轮只动登录/脱敏/租户详情，不触碰检索/向量/会话/文档隔离）。（`go test ./...` 全量含 `TestGetSessionMessagesReadsMySQL` 等多租户用例全绿；`GET /tenant/:id` 反而补强了租户归属校验）

## 八、范围外（明确不做）

- 管理后台的**用户管理 / 创建员工**（后端无接口，需求方已确认本轮不做）。
- **流式输出（SSE）**（后端无此能力，需求方已确认可接受普通请求）。
- 前端构建工具 / 组件框架（坚持零构建方案）。
- 会话重命名、消息编辑/删除、文档在线预览。
- 租户名的公开解析接口（登录仍只认用户名；租户名仅在登录后私有接口查询展示）。

## 九、上线前注意

- 前端为纯静态文件：`web/` 目录整体挂载到 Nginx，`/api` 与 `/health` 反代到后端 `SERVER_HTTP_PORT`（默认 8080）。
- 登录接口 `tenant_id` 变可选后**向后兼容**：老调用方（e2e 脚本）继续传 tenant_id 行为不变。
- `User.PasswordHash` 加 `json:"-"` 后所有返回 user 的接口自动脱敏，无字段名变化（前端不读该字段）。
- CORS：`cors.Default()` 允许任意来源，开发期前端不同端口可直连；生产走 Nginx 同源反代后 CORS 不涉及。

---

## 十、实现与验证记录（2026-08-19 新增）

> 本节为需求单从"已立项，待实现"推进为"✅ 已完成并验证"后的落地记录，与方案设计、自测清单一一对应。

### 8.1 状态结论

需求单 0004 **已实现并通过后端验证、独立/安全复核**，已提交推送 `origin/main`：

- **后端登录体验改造**：`LoginReq.TenantID` 改 `omitempty`；`service.Login` 支持 `tenantID==0` 的纯用户名全局登录（唯一登录/同名冲突报错/不存在统一"用户名或密码错误"），老调用（传 tenant_id）完全兼容；新增 `storage.FindUsersByUsername`。
- **响应脱敏**：`model.User.PasswordHash` 加 `json:"-"`，登录/注册/注册租户返回的 `user` 对象不再泄露密码哈希。
- **租户详情归属校验补强**（独立安全审查发现并修复）：`GetTenantDetail` 路径 id 必须等于 JWT `tenant_id`，否则返回"租户不存在"，堵住既有「任意登录用户可查任意租户详情」的 IDOR（登录态前端会常态调用它拉租户名）。
- **前端（`web/`，零构建纯静态）**：`login.html`（登录+租户注册，仅用户名密码登录，注册后自动登录）、`index.html`（三栏工作台：会话/对话含工具调用过程/文档管理含拖拽上传+任务轮询+删除）、`admin.html`（工具开关+当天/7天用量，仅 admin 可见入口）；配套 `api.js`（fetch 封装：自动带 token、401 跳登录且登录接口 401 message 透传、`code!=0` 业务错误判定）、`auth.js`（登录态/角色）、`chat.js`、`document.js`、`admin.js`、`css/style.css`。
- **部署**：`deploy/nginx.conf`（`location ^~ /api/` 反代到 Gin 8080，避免 `.js/.css` 结尾的 API 路径被静态 location 劫持）。
- **前端字段映射**：后端 `model.User`/`model.Tenant`/`model.Session` 等无 json tag，序列化字段名为大写（`Username/Role/TenantID/Name`）；前端已按此逐一配对，与 `gin.H` 的小写工具字段（`tool_name`/`is_enable`/`tokens`/`session_id` 等）区分正确。

### 8.2 验证证据

| 手段 | 结果 |
|------|------|
| 后端单测（`api/service/account_test.go` 新增 4 用例） | `TestLogin_ByUsername_Unique` / `TestLogin_ByUsername_Duplicate` / `TestLogin_ByUsername_NotFound` / `TestUser_PasswordHashOmitted` 全过 |
| `go test ./...` | 全量 29 个含测试/无测试包全部 `ok`，无 FAIL |
| `go vet ./...` | 全绿无告警 |
| `go build ./...` | 编译通过 |
| 前端 JS 语法 | 5 个 `web/js/*.js` `node --check` 全部通过；三个页面内联 `<script>` 也经 `node --check` 校验 |
| 独立 review | `ship as-is`：前端与后端所有接口请求路径/字段映射逐一核对一致，无 blocking 问题 |
| security-review | 无 SQL 注入/鉴权绕过/租户隔离破坏/XSS；`PasswordHash` 脱敏有效；`GET /tenant/:id` 归属校验生效；发现并确认修复 `admin.html` 角色判断 `u.role`→`u.Role` |
| 提交记录 | `383f4f3`（docs）+ `0d0ca66`（feat，17 文件 1380 行），已推送 `origin/main`，工作区干净、与远程同步 |

### 8.3 遗留 / 待办（如实记录）

- **前端浏览器手工 e2e（需求单 §7.2）尚未实测**：需本地 `docker compose up -d` → `go run cmd/migrate` → 起 `api` 后，用浏览器（或经 Nginx）验证登录/注册→对话→上传文档→admin 工具开关/用量。当前验证覆盖到"语法 + 后端测试 + 接口字段映射人工核对 + 独立/安全审查"，真实浏览器交互待本地实测后打勾。
- 既有取舍（范围外，README 上线清单已列）：登录/注册公开端点无爆破限流；`localStorage` 存 JWT + 第三方 CDN 无 SRI + nginx `listen 80` 明文，生产需 HTTPS；多租户同名登录返回存在性提示属 0004 有意设计。
