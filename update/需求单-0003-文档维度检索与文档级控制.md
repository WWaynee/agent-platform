# 需求单 0003：文档维度检索 + 文档级控制能力（按文档过滤 / 文档身份透传 / 整文档语境）

- 状态：📋 **已立项，待实现**（需求收集与方案设计完成，尚未开工）
- 优先级：🟡 中（补齐 RAG 检索的"文档维度"能力，解决"在所有片段里搜噪声大 / LLM 不知道来源 / 拿不到整文档语境"三大短板；为文档级分析（对比/总结/定位具体文档）铺路）
- 模块：`storage`（Qdrant payload / 检索过滤 / 文档搜索）、`api/service`（检索与文档搜索）、`toolkit`（三个新工具 + 改造知识库工具）、`agent/engine`（System Prompt）、`cmd/api`（工具注册 / 默认工具配置）
- 创建日期：2026-08-17

> 设计要点（相比现状的核心变化）：
> ① 检索从"全租户所有片段里搜"升级为**可按 `document_ids` 限定在某几个文档里搜**，且 LLM 知道每个片段来自哪个文档（`document_id` + `document_name` + `chunk_index`）；
> ② 新增**文档列表 / 文档全文 / 文档名搜索**三个工具，让 LLM 先"知道有哪些文档 → 匹配名称得 ID → 限定检索 → 必要时读全文"，从而承载"对比文档 A 与 B""总结文档 A"这类整文档级问题；
> ③ 用 **A+B 结合的"名称→ID 解析"**：文档少直接 list_documents 全量、LLM 自己匹配；文档多时用 search_documents 按关键字先搜再匹配；
> ④ 多租户隔离底线不变：所有新检索/搜索仍强制 `tenant_id` 过滤，新增 `document_ids` 过滤只是**叠加**在租户过滤之上，绝不削弱隔离。

---

## 一、功能现状与缺失

当前知识库检索是**全租户平铺式**：一次提问会把"当前租户所有文档的所有片段"混在一起检索，返回的每条命中只有 `content / score / document_id / chunk_index`，没有文档名，也没有"只在这几个文档里搜"的能力。由此产生三大缺失：

| 缺失能力 | 导致的问题 | 典型问不出来的问题 |
|----------|-----------|--------------------|
| 按文档过滤 | 每次都在全租户所有片段里搜，噪声大 | "对比文档 A 和文档 B 的差异" |
| 文档身份透传 | LLM 不知道片段来自哪个文档，无法引用来源、无法区分不同文档观点 | "文档 A 里怎么说，文档 B 里怎么说" |
| 整文档语境 | 只有碎片，看不到文档结构和完整上下文 | "总结一下文档 A 的核心观点" |

### 1. 检索链路现状

`toolkit/knowledge_retrieve.go` 的 `KnowledgeRetrieveTool.Execute`：

```go
// 当前只解析 query，没有 document_ids；topK 固定 3
var req struct {
	Query string `json:"query"`
}
hits, err := service.Search(ctx.ToContext(nil), ctx.TenantID, req.Query, 3)
// 拼回给 LLM 时只带 content + DocumentID
fmt.Fprintf(&b, "[%d] 文档ID=%d 相关度=%.3f\n%s\n\n", i+1, h.DocumentID, h.Score, h.Content)
```

### 2. 向量写入现状（payload 已含 document_id / chunk_index / tenant_id，缺 document_name）

`storage/qdrant.go` 的 `toPointStruct` 已把 `tenant_id / document_id / chunk_index / content` 写进 payload，但**没有 `document_name`**——LLM 无法知道片段属于哪篇文档（只能靠数字 ID 反查）。文档名是检索结果展示/引用来源的关键，需补齐。

### 3. 缺失的文档级工具

当前只有 `knowledge_retrieve` 一个工具。LLM 无法"先看有哪些文档"，也无法"读某篇文档全文"，因此对"合同 A 里的付款条款""对比两份合同"这类问题无从下手，只能硬把用户提的自然语言文档名当成 query 全库检索，噪声大且无法区分。

---

## 二、改进目标

分三阶段落地，目标依次为：

### 阶段 1（必做）：给检索加上"文档维度"
1. 确认/补齐 Qdrant payload：`document_id`（已有）、`document_name`（**新增**）、`chunk_index`（已有）、`tenant_id`（已有）。
2. 改造 `knowledge_retrieve` 工具入参，增加**可选** `document_ids` 过滤参数。
3. Qdrant 查询时在 `tenant_id` 过滤之上叠加 `document_id in [...]` 过滤。
4. 检索结果返回带上 `document_id / document_name / chunk_index`，LLM 知道每个片段来自哪篇文档、可引用来源。
5. 更新工具 `Description`，告诉 LLM 可以传 `document_ids`。

### 阶段 2（推荐）：增加"文档级"控制能力
1. 新增 `list_documents` 工具：返回当前租户所有文档（id/name/size/status/时间），供 LLM 先知道有哪些文档。
2. 新增 `get_document_content` 工具：返回某篇文档的完整文本（**带长度限制**，超过阈值截断或只返回前 N 片段 / 摘要 + 关键片段）。
3. 新增 `search_documents` 工具（按名称 `LIKE` 模糊搜索，文档多时用）。
4. （可选）文档处理完成后预生成摘要存 `documents.summary`，供 list_documents 返回、帮 LLM 选文档。
5. 三个工具注册进 ToolManager，并纳入新租户默认工具配置。

### 阶段 3（配合）：ReAct 侧编排
1. System Prompt 更新：告诉 LLM 新工具能力与"名称→ID 解析"的多文档推理路径。
2. 边界情况处理规则写入 Prompt：匹配不到 / 匹配到多个 / 用户未提具体文档时的正确行为。

---

## 三、方案设计

### 3.1 阶段 1：文档维度检索

#### （1）补齐 Qdrant payload：`document_name`

**`storage/qdrant.go`**

```go
type QdrantVector struct {
	ID           uint64    // 点全局唯一 ID
	TenantID     uint64    // 租户 ID（检索过滤键，隔离底线）
	DocumentID   uint64    // 文档 ID
	DocumentName string    // 文档名称（新增：检索结果随命中返回，LLM 据此引用来源）
	ChunkIndex   int       // 第几个切片
	Content      string    // 切片原文
	Vector       []float32 // 向量
}

func toPointStruct(v QdrantVector) (*qdrant.PointStruct, error) {
	// payload 增加 document_name（文本值）
	docNameVal, err := qdrant.NewValue(v.DocumentName)
	// ... 写入 payload["document_name"] = docNameVal
}
```

**`api/service/document_parse.go`** 的 `ProcessDocument` 组装 `QdrantVector` 时带上文档名：

```go
points = append(points, storage.QdrantVector{
	ID:           composePointID(documentID, i),
	TenantID:     tenantID,
	DocumentID:   documentID,
	DocumentName: doc.Name, // 新增：写入 payload，检索时随命中返回
	ChunkIndex:   i,
	Content:      chunk,
	Vector:       vectors[i],
})
```

> ⚠️ 迁移注意：`document_name` 只对**新写入**的向量生效。历史已入库的向量无此字段 → 检索结果里 `document_name` 为空。阶段1 落地后建议对存量文档重跑一次 `ProcessDocument`（或接受旧片段缺文档名、靠 `document_id` 反查表兜底）。

#### （2）`storage.SearchVectors` 支持可选 `document_ids` 过滤

在现有强制 `tenant_id` 过滤之上，叠加 `document_id in [...]`：

```go
// 在 tenantIDFilter 基础上，若传了 document_ids，再叠加 document_id 的 Match 任意值条件
func SearchVectors(ctx context.Context, query []float32, tenantID uint64, topK int, documentIDs ...uint64) ([]QdrantSearchHit, error) {
	conditions := []*qdrant.Condition{
		{ ConditionOneOf: &qdrant.Condition_Field{ Field: &qdrant.FieldCondition{
			Key: "tenant_id",
			Match: &qdrant.Match{ MatchValue: &qdrant.Match_Integer{ Integer: int64(tenantID) } },
		}}},
	}
	if len(documentIDs) > 0 {
		ids := make([]int64, 0, len(documentIDs))
		for _, id := range documentIDs {
			ids = append(ids, int64(id))
		}
		conditions = append(conditions, &qdrant.Condition{ ConditionOneOf: &qdrant.Condition_Field{ Field: &qdrant.FieldCondition{
			Key: "document_id",
			Match: &qdrant.Match{ MatchValue: &qdrant.Match_Integers{
				Integers: &qdrant.RepeatedIntegers{ Integers: ids },
			}},
		}}}) // document_id in [1,2,3]
	}
	req.Filter = &qdrant.Filter{ Must: conditions }
}
```

- `documentIDs` 用可变参数，**不传即为全租户检索（兼容老逻辑）**。
- 过滤条件用 qdrant-go 的 `Match_Integers`（整型字段的"in 列表"匹配），配合 `qdrant.RepeatedIntegers` 传 `int64` 列表。
- `tenant_id` 过滤仍由本函数内部强制构造（传进来也不信，隔离底线 stays）——`document_ids` 只是在此之上再收敛范围；即使误传别的租户的文档 ID，也会被 `tenant_id` 过滤兜住。

#### （3）`api/service/knowledge.go`：`Search` 增加 `documentIDs`，`SearchHit` 带文档名

```go
type SearchHit struct {
	Content      string  `json:"content"`
	Score        float32 `json:"score"`
	DocumentID   uint64  `json:"document_id"`
	DocumentName string  `json:"document_name"` // 新增：文档名称
	ChunkIndex   int     `json:"chunk_index"`
}

func Search(ctx context.Context, tenantID uint64, query string, topK int, documentIDs ...uint64) ([]SearchHit, error) {
	// 检索时透传 documentIDs
	hits, err := storage.SearchVectors(ctx, resp.Vector, tenantID, topK, documentIDs...)
	// 组装时从 payload 取 document_name
	out = append(out, SearchHit{
		Content: h.Content, Score: h.Score,
		DocumentID:   payloadUint64(h.Payload, "document_id"),
		DocumentName: payloadString(h.Payload, "document_name"), // 新增取文本
		ChunkIndex:   int(payloadUint64(h.Payload, "chunk_index")),
	})
}

// payloadString 从 payload 安全取字符串字段；缺失返回空串。
func payloadString(payload map[string]interface{}, key string) string
```

#### （4）`toolkit/knowledge_retrieve.go`：入参加 `document_ids`，返回带上文档信息

```go
func (KnowledgeRetrieveTool) Parameters() string {
	return `{
		"type": "object",
		"properties": {
			"query": {
				"type": "string",
				"description": "要检索的问题或关键词，尽量说明确、具体，以获得准确结果"
			},
			"document_ids": {
				"type": "array",
				"items": { "type": "integer" },
				"description": "可选。指定只在这些文档里检索（传文档ID数组）。
					当用户提到具体文档名并已通过 list_documents/search_documents 得知其ID时，务必传入以限定范围，减少噪声。不传则在当前租户全部文档里检索。"
			},
			"top_k": {
				"type": "integer",
				"description": "可选。期望返回的片段条数，默认3"
			}
		},
		"required": ["query"]
	}`
}

// Execute 内解析 document_ids / top_k
var req struct {
	Query       string   `json:"query"`
	DocumentIDs []uint64 `json:"document_ids"`
	TopK        int      `json:"top_k"`
}
if req.TopK <= 0 { req.TopK = 3 }
hits, err := service.Search(ctx.ToContext(nil), ctx.TenantID, req.Query, req.TopK, req.DocumentIDs...)

// 返回格式带文档身份信息
fmt.Fprintf(&b, "[%d] 文档=%s(ID=%d 片段%d) 相关度=%.3f\n%s\n\n",
	i+1, h.DocumentName, h.DocumentID, h.ChunkIndex, h.Score, h.Content)
```

**返回格式（阶段1 起）：**

```json
[
  { "content": "片段内容", "score": 0.85, "document_id": 1, "document_name": "产品需求文档v2.0", "chunk_index": 3 }
]
```

#### （5）HTTP 检索接口（`api/handler/knowledge.go`）也支持可选 `document_ids`（与 Agent 工具对齐，便于联调）

```go
type SearchRequest struct {
	Query       string   `json:"query" binding:"required,min=1,max=500"`
	TopK        int      `json:"top_k"`
	DocumentIDs []uint64 `json:"document_ids"` // 可选：只在指定文档里检索
}
hits, err := service.Search(c.Request.Context(), tenantID, req.Query, req.TopK, req.DocumentIDs...)
```

---

### 3.2 阶段 2：文档级控制能力

#### （1）`storage/document.go` 新增按名称搜索 + 供词

```go
// SearchDocuments 按文档名模糊搜索（LIKE 关键字），强制 tenant_id 过滤、排除软删。
// 返回当前租户中名称含 keyword 的文档（按更新时间倒序）。
func SearchDocuments(ctx context.Context, tenantID uint64, keyword string) ([]model.Document, error) {
	query := DB.WithContext(ctx).Where("tenant_id = ?", tenantID)
	if keyword != "" {
		// GORM 参数化 LIKE，天然防注入（特殊字符 % _ 作为字面量处理，本需求接受该行为）
		query = query.Where("name LIKE ?", "%"+keyword+"%")
	}
	return query.Order("updated_at DESC").Find(&list)...
}

// GetDocumentText 供 get_document_content 读取某篇文档的全部文本（复用 ReadTextDocument）。
// 先带租户过滤取文档 → 用 minioKey+name 读 MinIO 全文。
type DocumentText struct {
	DocumentID   uint64
	DocumentName string
	Content      string // 全文（未截断前的原始文本，截断由工具层负责）
	TotalChars   int    // 原始总字符数
}
func ReadDocumentContent(ctx context.Context, tenantID, documentID uint64) (*DocumentText, error)
```

#### （2）新增 `toolkit/list_documents.go`：`ListDocumentsTool`

- 工具名 `list_documents`，入参无（或可选 `keyword`）。
- 返回当前租户全部文档：`[{id, name, size, status, created_at}]`（`WHERE tenant_id=? AND deleted_at IS NULL`，用 `storage.ListDocuments` 分页拉取或新增不分页全量方法）。
- `Description`：明确"当用户提到具体文档（如"合同A"）时，先调用此工具获取文档 ID；也用于让 LLM 了解企业有哪些文档、便于决定检索哪几篇。"
- 租户隔离：从 `ctx.TenantID` 取租户，`storage.ListDocuments` 已强制过滤。

#### （3）新增 `toolkit/search_documents.go`：`SearchDocumentsTool`

- 工具名 `search_documents`，入参 `keyword` 必填。
- 逻辑：`WHERE tenant_id=? AND name LIKE '%keyword%'` 模糊搜索，返回匹配文档 `[{id, name, size, status}]`。
- `Description`：明确"当知道文档的模糊名称（如'合同'）但文档很多、需要在名称里精确检索时用；文档少也可直接用 list_documents。"
- 空 keyword / 无匹配：返回空数组与提示，不报错。

#### （4）新增 `toolkit/get_document_content.go`：`GetDocumentContentTool`

- 工具名 `get_document_content`，入参 `document_id` 必填。
- 逻辑：带租户过滤取文档 → `storage.DownloadFile(minioKey)` 读全文 → **按长度限制截断**。
- 长度限制：超过 `MaxDocumentTokens`（建议 4000~8000 token，可按字符数近似 `~` token 数）时，**只返回前 N 段**并在末尾提示"文档过长，已截断到前 N 段，如需特定部分可用 knowledge_retrieve 精确检索"。

```go
const MaxDocumentChars = 4000 // 长度上限（近似 token，低于该值直接返回全文）
func (GetDocumentContentTool) Execute(ctx, params) (string, error) {
	var req struct{ DocumentID uint64 `json:"document_id"` }
	// 1. 带租户过滤读取全文（storage.ReadDocumentContent）
	txt, err := storage.ReadDocumentContent(ctx.ToContext(nil), ctx.TenantID, req.DocumentID)
	// 2. 超限截断前 4000 字符 + 提示；未超限全量返回
	// 3. 返回时注明文档名，便于 LLM 引用来源
	return fmt.Sprintf("文档《%s》(ID=%d) 内容：\n%s", txt.DocumentName, txt.DocumentID, body), nil
}
```

#### （5）注册到 ToolManager + 默认工具配置

- `cmd/api/main.go`：新增 `tm.RegisterTool(toolkit.NewListDocumentsTool())`、`tm.RegisterTool(toolkit.NewSearchDocumentsTool())`、`tm.RegisterTool(toolkit.NewGetDocumentContentTool())`。
- `storage/tool_config.go` 的 `DefaultTools` 追加三新工具名，使新租户默认开启。

```go
var DefaultTools = []string{
	"knowledge_retrieve",
	"list_documents",     // 新增
	"search_documents",   // 新增
	"get_document_content", // 新增
}
```

#### （6）（可选）文档摘要预生成

- `model.Document` 增 `Summary string gorm:"type:text"` 列（`AllModels` 自动加列）。
- `ProcessDocument` 成功写入向量后，额外调用一次 LLM `Chat` 生成文档摘要并 `UpdateDocumentSummary` 落 `documents.summary`。
- `list_documents` 可同时返回 `summary`，帮助 LLM 选文档；`get_document_content` 超长时也可优先返回摘要。

> 范围外说明：摘要预生成涉及额外 LLM 调用与失败降级，本期可先留"可选"位，优先级从低到高。

---

### 3.3 名称→ID 解析（A+B 结合）

用户说"合同 A 是自然语言名称，`knowledge_retrieve` 要的是数字 ID"，中间需要一步"名称解析"。

**方案 A（文档少 <50 篇）：`list_documents` 全量返回，LLM 自己匹配**
- 用户"合同A里的付款条款是什么？"→ LLM 调 `list_documents` → 拿到 `[{id,name,...}]` → LLM 把"合同A"匹配到 `id` → 调 `knowledge_retrieve(document_ids=[id], query="付款条款")`。

**方案 B（文档多 ≥50 篇）：`search_documents` 按名称模糊搜**
- 用户"合同A里的付款条款？"→ LLM 调 `search_documents(keyword="合同A")` → 拿到匹配 `[{id,name}]` → 匹配到 `id` → 调 `knowledge_retrieve(document_ids=[id])`。

**结合策略**：文档少时 LLM 自然用 list_documents 全量即可；文档多时用 search_documents 先搜再匹配。两者都提供，由 System Prompt 引导 LLM 按文档数量权衡。

---

### 3.4 阶段 3：ReAct 侧编排 & System Prompt

`agent/engine/prompt.go` 的规则段追加文档维度说明（在"四、规则"后增加一节）：

```
五、文档维度检索规则：
1. 当用户提到具体文档名称时（如"合同A"、"产品文档"、"员工手册"）：
   a. 先用 list_documents（文档不多时）或 search_documents（文档多、需按名称搜时）拿到文档 ID
   b. 再调 knowledge_retrieve 并传 document_ids 参数，限定只在该文档里检索，减少噪声
2. 若用户提到的文档名匹配不到或匹配到多个：
   - 不要瞎猜 / 不要编造文档 ID；
   - 主动列出相近的文档（id + name），请用户确认"你指的是哪个？"
   - 或（仅在能覆盖时）不带 document_ids 全量检索
3. 若用户未提具体文档（如"付款条款一般怎么写"）：可直接全量检索（不传 document_ids），
   或基于通用知识回答，不强求匹配文档。
4. 当你需要某篇文档的完整语境（如"总结文档A核心观点""对比文档A和B"）且片段不够时，
   可调 get_document_content 读取整篇文档。
5. 回答中若引用了文档内容，注明来源文档名称（如"根据《产品需求文档v2.0》……"），
   多文档对比时分别标注各文档的观点，不要混为一谈。
```

**多文档问题（如"对比文档 A 和文档 B"）的典型推理路径**（写入 Prompt 正例）：

```
用户："对比采购合同和销售合同A在付款条款上的区别"
LLM 思考：用户提到两个具体文档 → 先找它们 ID
→ 调 list_documents（或 search_documents "合同"）
→ 拿到 采购合同(id=1)、销售合同A(id=2)
→ 调 knowledge_retrieve(document_ids=[1,2], query="付款条款")
→ 观察返回片段，综合对比，回答时分别标注"采购合同里说……销售合同A里说……"
```

---

## 四、涉及文件清单（预期改动）

| 文件 | 改动类型 |
|------|---------|
| `storage/qdrant.go` | **改造**：`QdrantVector` 加 `DocumentName`；`toPointStruct` 写 `document_name` payload；`SearchVectors` 增加可选 `documentIDs` 过滤（可变参数）|
| `api/service/document_parse.go` | **改造**：`ProcessDocument` 组装 `QdrantVector` 带 `DocumentName: doc.Name`；（可选）成功后生成摘要落 `summary` |
| `api/service/knowledge.go` | **改造**：`SearchHit` 加 `DocumentName`；`Search` 增加 `documentIDs` 可变参数并透传；新增 `payloadString` |
| `api/handler/knowledge.go` | **改造**：`SearchRequest` 加可选 `document_ids`；透传 |
| `toolkit/knowledge_retrieve.go` | **改造**：入参加 `document_ids/top_k`；返回带文档信息；`Description` 更新 |
| `storage/document.go` | **新增**：`SearchDocuments`（名称 LIKE / 租户过滤）；`ReadDocumentContent`（带租户过滤读全文）→ 供新工具 |
| `toolkit/list_documents.go` | **新增**：`ListDocumentsTool`（返回当前租户文档列表）|
| `toolkit/search_documents.go` | **新增**：`SearchDocumentsTool`（按名称模糊搜）|
| `toolkit/get_document_content.go` | **新增**：`GetDocumentContentTool`（读全文 + 长度截断）|
| `cmd/api/main.go` | **改造**：注册三个新工具到 ToolManager |
| `storage/tool_config.go` | **改造**：`DefaultTools` 追加 `list_documents/search_documents/get_document_content` |
| `storage/model/models.go` | **改造（可选）**：`Document` 加 `Summary` 列；`AllModels` 覆盖 |
| `agent/engine/prompt.go` | **改造**：规则段追加"文档维度检索规则"全新一节 + 多文档推理路径正例 |
| `api/service/tool_permission.go` | **无改动**（新工具沿用"查不到默认放行"，老租户不会误拦截；新租户经 `DefaultTools` 显式开启）|
| `scripts/e2e_document_analysis.sh` | **新增**：端到端脚本（文档维度检索 + 工具编排 + 名称解析 + 边界场景）|
| `docs/db.md` | **更新（可选）**：`documents.summary` 列说明 |
| `README.md` | **更新**：文档维度检索 / 文档级控制能力说明 |

---

## 五、自测任务

### 5.1 阶段 1：文档维度检索

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| payload 含文档信息 | 上传文档→向量化→查 Qdrant 点 payload | 每个点含 `tenant_id/document_id/document_name/chunk_index/content` |
| 不传 document_ids | 调 knowledge_retrieve（单传 query）| 全租户检索（**兼容老逻辑**）|
| 传 document_ids | `document_ids=[2]` | 只返回文档 2 的片段 |
| 多文档过滤 | `document_ids=[1,2]` | 返回文档 1 和 2，不含文档 3 |
| 传不存在的 ID | `document_ids=[999]` | 返回空，不报错 |
| 传其他租户的 ID | 传租户 B 的文档 ID | 返回空（`tenant_id` 过滤兜底）|
| 返回带文档信息 | 任意检索 | 结果里有 `document_id/document_name/chunk_index` |
| 历史向量兼容 | 存量向量无 document_name | 检索结果 `document_name` 为空，不崩、可回退靠 document_id |

### 5.2 阶段 2：文档级工具

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| list_documents 正常 | 上传 3 篇文档后调用 | 返回 3 条，含 id/name/size/status |
| list_documents 租户隔离 | 用租户 B 调用 | 只看到 B 的文档 |
| list_documents 空列表 | 新租户无文档 | 返回空数组，不报错 |
| list_documents 软删 | 软删除一篇文档后 | 不返回已删除的 |
| search_documents 精确/模糊 | keyword="销售合同A" / "合同" | 精确返回目标；模糊返回多篇 |
| search_documents 无匹配 | keyword="劳动合同"（不存在）| 返回空数组，不报错 |
| search_documents 防注入 | keyword 含 `%/_` / 引号 | GORM 参数化，无 SQL 异常 |
| get_document_content | 传 document_id | 返回整篇全文 + 文档名 |
| get_document_content 超长 | 长文档 | 截断到上限 + 明确提示"已截断" |

### 5.3 阶段 3：LLM 编排

| 测试问法 | 预期 LLM 行为 |
|----------|--------------|
| "销售合同A里的付款条款是什么？" | 先 list/search 拿 ID → 传 document_ids=[id] 检索 → 回答并引用《销售合同A-2024版》 |
| "合同里的违约条款怎么说的？"（多匹配） | 不瞎猜：要么列出多个让用户确认，要么全量检索；不报错 |
| "劳动合同里的试用期规定？"（不存在）| 不编造：列出已有文档请用户确认，不瞎答 |
| "付款条款一般怎么写？"（未提文档）| 可全量检索或通用回答，不强匹配文档 |
| "对比采购合同和销售合同A的付款条款" | 匹配两个文档 → document_ids=[1,2] → 分别标注来源对比回答 |
| "总结一下文档A的核心观点" | 片段不够时调 get_document_content 读全文后总结 |

### 5.4 回归
- [ ] 多租户隔离硬性对抗不回归：`document_ids` 过滤是叠加在 `tenant_id` 之上，绝不削弱隔离；`storage.SearchVectors` 强制 `tenant_id` 过滤零改动核心。
- [ ] 既有主流程回归：上传文档→向量化→RAG 问答（不带 document_ids）仍正常。
- [ ] `go vet ./...` / `go test ./...` / `go build ./...` 全绿。

---

## 六、范围外（明确不做）

- 向量检索的多字段排序 / 分页 / 高亮（本期只需"可限定文档 + 带身份信息"）。
- 文档权限的**字段级 / 片段级**控制（沿用整文档租户级隔离）。
- 文档摘要预生成的**异步任务化 / 失败重试**（可选做同步生成 + 尽力而为，重试机制范围外）。
- `search_documents` 的语义搜索（名称检索本期用 DB `LIKE`，不做向量化名称匹配；如需可后续演进）。
- 会话 / 审计的文档维度扩展（本期不改）。
- 存量向量 document_name 的**自动批量回填**（阶段1 落地后需手动重跑 ProcessDocument 或接受缺文档名兜底）。

---

## 七、上线前注意

- **阶段 1 涉及存量数据**：`document_name` 只对新写入向量生效；上线后建议对存量文档重跑一次向量化（`worker` 重投 document_parse 或调用 `ProcessDocument`），否则旧片段 `document_name` 为空。已做的兜底：检索时 `document_name` 缺失返回空串、不崩，可回退用 `document_id` 反查 `documents` 表。
- **`storage.SearchVectors` 签名变化**：从 4 参变 5 参（新增 `documentIDs` 可变参数）。由于采用 **Go 可变参数（`...`）**，现有调用 `SearchVectors(ctx, v, tid, topK)` 无需改动也能编译（兼容），业务调用零破坏。
- **`DefaultTools` 变化**：新增两个工具后，新租户默认开启；老租户因"查不到默认放行"策略**不会误拦截**新工具，无需手工补配置。
- **工具权限**：三个新工具纳入 `ToolManager` 统一执行，天然过 `DBPermissionChecker`（PermissionChecker 接口无需改动）。
- **`get_document_content` 长度上限**：务必按 token/字符上限截断，并把"已截断 + 如需精确部分建议用 knowledge_retrieve"提示给 LLM，避免整本塞爆上下文 / 超窗。
